notion:
  # Notion sync is auto-enabled when NOTION_API_KEY and NOTION_TASK_DB_ID are set in .env
  task_database_id: ""

  property_map:
    title: Title
    description: Description
    deadline: Deadline
    module: Module
    source: Source
    status: Status

  status_options:
    todo: todo
    doing: doing
    done: done
